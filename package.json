{
  "name": "access-code-hub",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:client": "cd packages/client && vite",
    "dev:server": "cd packages/server && npm run dev",
    "dev": "concurrently -n client,server -c green,blue \"npm run dev:client\" \"npm run dev:server\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "tsx": "^4.20.6",
    "vite": "^7.3.1"
  }
}